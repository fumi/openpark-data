# Dataset for Openpark

## Setup

```
$ cd bin
$ cp config.sh.sample config.sh
```

config.shのDATADIRを絶対パスで指定、DBA_PASSWORDを任意のパスワードに変更


```
$ ./docker-run
$ ./docker-bash
# ./import
# exit
```

これでデータインポートしたVirtuosoのdockerインスタンスが上がる。

## TODO
もっと簡単なデプロイを
